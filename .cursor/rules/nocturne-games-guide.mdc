---
description: Nocturne Games 长期开发规范 — 以 Project Guide 为准
alwaysApply: true
---

# Nocturne Games 开发规范（强制）

## 唯一权威

长期工程规范以本仓库文件为准：

`docs/Nocturne_Games_Project_Guide.md`

- 以后所有开发默认遵守该 Guide。
- **不要**要求用户每次重新粘贴或提供 Guide。
- 若发现 Guide 有更新，先阅读最新 Guide，再按其中条款执行；并把要点同步进本规则（本文件）。
- 游戏内模块细节仍以各子项目 `ARCHITECTURE.md` / `CHANGELOG.md` 补充，但不得与 Guide 冲突。

## 必须遵守的摘要

- 定位：长期维护的游戏平台；GitHub Pages 直开；原生 HTML/CSS/JS ES Module；**PWA 离线**（`docs/PWA.md`）。
- 禁止：React / Vue / Angular / TypeScript / npm / 外部麻将库 / Workbox。
- 麻将职责：`game.js` 规则状态 AI；`render.js` DOM/UI/动画；`tiles.js` SVG；`hu.js` 胡牌；`storage.js` 存档；`config.js` 配置。
- SW 注册固定：`shared/pwa.js` 内 `new URL("../service-worker.js", import.meta.url)`；改预缓存须 bump `nocturne-games-vN`；不碰 localStorage。
- 小改直接编码；大改（约 300+ 行 / 数据结构 / 流程 / 新模块）先给方案、文件、风险、测试点，确认后再写。
- 完成后输出：修改文件、修改内容、新增函数、流程变化、数据结构变化、测试建议、已知问题。
- 收工必须跑 `python scripts/verify.py`，并更新 `docs/test-results/latest-test-result.md`（见 `development-workflow.mdc`）。
- 优先级：可读性 > 可维护性 > 扩展性 > 功能数量。
- Debug Mode（如规则测试）仅 localhost，正式环境隐藏。
- 项目记忆以 Git 内 `docs/` 与 `.cursor/rules/` 为准，不以聊天记录为准。

## Guide 更新时

1. 读 `docs/Nocturne_Games_Project_Guide.md`
2. 更新本规则中与 Guide 不一致的摘要
3. 按新 Guide 继续开发
